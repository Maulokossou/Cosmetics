import './bootstrap';

import Alpine from 'alpinejs';

window.Alpine = Alpine;

Alpine.start();

import { Tooltip, Collapse,   Carousel,initTWE,  Ripple } from "tw-elements";


initTWE({ Tooltip,  Collapse,  Carousel,  Ripple });
